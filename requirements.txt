fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
aiofiles==24.1.0
google-genai==1.2.0
anthropic==0.116.0
PyYAML==6.0.2
truststore==0.10.4

# Layer 1 - Feature Extraction
PyMuPDF==1.25.2
python-pptx==1.0.2
librosa==0.10.2.post1
numpy==1.26.4
soundfile==0.12.1
opencv-python-headless==4.10.0.84

# Layer 2 - AI Vision & Speech Intelligence
openai-whisper==20240930
torch==2.5.1
# mediapipe covers two analyzers: Face Mesh (analyzers/facemesh_analyzer.py)
# and Pose/BlazePose (analyzers/pose_analyzer.py). Both use the legacy
# `mp.solutions` API rather than Holistic or the Tasks API -- see the module
# docstring in analyzers/pose_analyzer.py for why. No .task model file needs
# shipping: the legacy solutions bundle their own weights, so this runs offline.
mediapipe==0.10.14
hsemotion-onnx==0.3.1
onnxruntime==1.19.2

# Testing
pytest==8.3.4
httpx==0.28.1
pytest-asyncio==0.24.0

# Session persistence (PostgreSQL)
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# Accounts (Nhóm B): password hashing + JWT + email format validation
bcrypt==5.0.0
PyJWT==2.9.0
email-validator==2.3.0

# Recommendation Engine catalog seeding (scripts/seed_learning_resources.py)
pandas==2.2.3
openpyxl==3.1.5
